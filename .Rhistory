rexp(10, 0.2)
?rexp
set.seed(42)
pop <- rexp(n = 1000, rate = 0.2)
plot(pop)
hist(pop)
?abline
hist(pop) + abline(h = mean(pop))
mean(pop)
?hist
?abline
hist(pop)
abline(h = mean(pop))
abline(v = mean(pop))
abline(v = mean(pop), col == "red"")
abline(v = mean(pop), col == "red")
abline(v = mean(pop), col = "red")
?text
text(mean(pop), -1, mean(pop))
3
text(mean(pop), -3, mean(pop))
text(mean(pop), -10, mean(pop))
text(mean(pop), -20, mean(pop))
round(mean(pop),2)
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col == "red")
text(mean(pop), -20, mean(pop))
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col = "red")
text(mean(pop), -20, mean(pop))
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col = "red")
text(mean(pop), -20, round(mean(pop,2))
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col = "red")
text(mean(pop), -20, round(mean(pop,2)))
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col = "red")
text(mean(pop), -20, round(mean(pop,2)))
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col = "red")
text(mean(pop), -50, round(mean(pop,2)))
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col = "red")
text(mean(pop), 400, round(mean(pop,2)))
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col = "red")
text(mean(pop), 400, round(mean(pop),2))
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col = "red")
text(mean(pop), 700, round(mean(pop),2))
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col = "red")
text(mean(pop), 630, round(mean(pop),2))
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col = "red")
text(mean(pop), 550, round(mean(pop),2))
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col = "red")
text(mean(pop), 450, round(mean(pop),2))
?text
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col = "red")
text(mean(pop), 450, round(mean(pop),2), pos = 4)
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col = "red")
text(mean(pop), 100, round(mean(pop),2), pos = 4)
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col = "red")
text(mean(pop), 450, round(mean(pop),2), pos = 4)
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col = "red")
text(mean(pop), 450, paste("-",round(mean(pop)),2), pos = 4)
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col = "red")
text(mean(pop), 450, paste("-",round(mean(pop),2)), pos = 4)
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col = "red")
text(mean(pop), 450, paste("<-",round(mean(pop),2)), pos = 4)
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col = "red")
text(mean(pop), 450, paste("<--",round(mean(pop),2)), pos = 4)
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col = "red")
text(mean(pop), 450, paste("<-",round(mean(pop),2)), pos = 4)
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col = "red")
text(mean(pop), 450, paste("<--",round(mean(pop),2)), pos = 4)
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(pop), col = "red")
text(mean(pop), 450, round(mean(pop),2), pos = 4)
?var
set.seed(42)
population <- rexp(n = 1000, rate = 0.2)
hist(population)
abline(v = mean(population), col = "red")
text(mean(population), 450, round(mean(population),2), pos = 4)
var(population)
sd(population)
sqrt(var(population))
rexp(1000 * 40, 0.2)
?matrix
matrix(rexp(40 * 1000, 0.2), ncol = 40)
test <- matrix(rexp(40 * 1000, 0.2), ncol = 40)
View(test)
?apply
apply(matrix(rexp(40 * 1000, 0.2), ncol = 40), 1, "mean")
set.seed(42)
population <- apply(matrix(rexp(40 * 1000, 0.2), ncol = 40), 1, "mean")
hist(population)
set.seed(42)
sample.means <- apply(matrix(rexp(40 * 1000, 0.2), ncol = 40), 1, "mean")
hist(sample.means)
abline(v = mean(sample.means), col = "red")
text(mean(sample.means), 150, round(mean(sample.means),2), pos = 4)
set.seed(42)
samples <- matrix(rexp(40 * 1000, 0.2), ncol = 40)
sample.means <- apply(samples, 1, "mean")
sample.vars <- apply(samples, 1, "var")
hist(sample.vars)
?var
var(sample)
set.seed(42)
samples <- matrix(rexp(40 * 10000, 0.2), ncol = 40)
sample.means <- apply(samples, 1, "mean")
sample.vars <- apply(samples, 1, "var")
hist(sample.vars)
set.seed(42)
samples <- matrix(rexp(40 * 1000, 0.2), ncol = 40)
sample.means <- apply(samples, 1, "mean")
sample.vars <- apply(samples, 1, "var")
?rexp
mean(sample.means)
1/0.2
#Theoretical mean is:
1 / 0.2
#Mean of 1000 samples of size 40 from exponential distribution is:
mean(sample.means)
var(sample.means)
(1/0.2)^2
sqrt(1/0.2)
(1/0.2)/40
var(sample.means)
(1/0.2)^2/40
(1/0.2)^2/39
#Theoretical sample variance is:
(1/0.2^2)/40
#Now compute it directly from sample.means
var(sample means)
#Theoretical sample variance is:
(1/0.2^2)/40
#Now compute it directly from sample.means
var(sample.means)
set.seed(42)
samples <- matrix(rexp(n = 40 * 10000, rate = 0.2), ncol = 40)
sample.means <- apply(samples, 1, "mean")
#Theoretical sample variance is:
(1/0.2^2)/40
#Now compute it directly from sample.means
var(sample.means)
hist(sample.means)
??lines
?lines
plot(cars, main = "Stopping Distance versus Speed")
lines(stats::lowess(cars))
hist(sample.means)
?rnorm
lines(rnorm(mean = mean(sample.means), sd = sd(sample.means)))
lines(rnorm(1000, mean = mean(sample.means), sd = sd(sample.means)))
set.seed(42)
samples <- matrix(rexp(n = 40 * 1000, rate = 0.2), ncol = 40)
sample.means <- apply(samples, 1, "mean")
#Theoretical mean is:
1 / 0.2
#Mean of 1000 samples of size 40 from exponential distribution is:
mean(sample.means)
#Theoretical sample variance is:
(1/0.2^2)/40
#Now compute it directly from sample.means
var(sample.means)
hist(sample.means)
lines(rnorm(1000, mean = mean(sample.means), sd = sd(sample.means)))
sd(sample.means)
sd(sample.means)^2
mean(sample.means)
?lines
?pnorm
set.seed(42)
hist(sample.means)
lines(x = rnorm(1000, mean = mean(sample.means) y = pnorm(1000, mean = mean(sample.means), sd = sd(sample.means)))
set.seed(42)
hist(sample.means)
lines(x = rnorm(1000, mean = mean(sample.means), y = pnorm(1000, mean = mean(sample.means), sd = sd(sample.means)))
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
norm.distr <- rnorm(1000, mean = mean(sample.means), sd = sd(sample.means))
lines(x = norm.distr, y = pnorm(norm.distr))
pnorm(norm.distr)
dnorm(norm.distr)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
norm.distr <- rnorm(1000, mean = mean(sample.means), sd = sd(sample.means))
lines(x = norm.distr, y = dnorm(norm.distr))
seq(1, 8)
seq(1, 8, length = 100)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
x <- seq(1, 8, length = 1000)
y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
lines(x = , y = dnorm(norm.distr))
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
x <- seq(1, 8, length = 1000)
y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
lines(x, y)
?curve
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means)))
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means)), add = TRUE, yaxt = "n")
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
x <- seq(1, 8, length = 1000)
y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
lines(x, y)
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means)), add = TRUE, yaxt = "n")
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means)), add = TRUE, yaxt = "n", lwd=2)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
x <- seq(1, 8, length = 1000)
y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means)), add = TRUE, yaxt = "n", lwd=2)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
x <- seq(1, 8, length = 1000)
y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means)), add = TRUE, yaxt = "n", lwd=2)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
#x <- seq(1, 8, length = 1000)
#y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means)), add = TRUE, yaxt = "n", lwd=2)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
#x <- seq(1, 8, length = 1000)
#y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means)), add = TRUE, yaxt = "n", lwd=2)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
#x <- seq(1, 8, length = 1000)
#y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
curve(dnorm(z, mean = mean(sample.means), sd = sd(sample.means)), add = TRUE, yaxt = "n", lwd=2)
rm(x)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
#x <- seq(1, 8, length = 1000)
#y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means)), add = TRUE, yaxt = "n", lwd=2)
mean(sample.means)
sd(sample.means)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
#x <- seq(1, 8, length = 1000)
#y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means)), add = TRUE, yaxt = "n", lwd=2)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
#x <- seq(1, 8, length = 1000)
#y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means)), add = TRUE)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
#x <- seq(1, 8, length = 1000)
#y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
curve(dnorm(x, 5, 0.6, add = TRUE)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
#x <- seq(1, 8, length = 1000)
#y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
curve(dnorm(x, 5, 0.6), add = TRUE)
?dnorm
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
#x <- seq(1, 8, length = 1000)
#y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means)), 1,8,add = TRUE, yaxt = "n", lwd=2)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
#x <- seq(1, 8, length = 1000)
#y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means)), 1,8,add = TRUE, yaxt = "n", lwd=2)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
#x <- seq(1, 8, length = 1000)
#y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
curve(x, 1,8,add = TRUE, yaxt = "n", lwd=2)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
#x <- seq(1, 8, length = 1000)
#y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
curve(x*1, 1,8,add = TRUE, yaxt = "n", lwd=2)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
#x <- seq(1, 8, length = 1000)
#y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
curve(x*1, 1,8,add = TRUE)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
#x <- seq(1, 8, length = 1000)
#y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
curve(x*1, 1,8,add = TRUE, lwd=2)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
#x <- seq(1, 8, length = 1000)
#y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
curve(x^2, 1,8,add = TRUE, lwd=2)
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means)), 1,8, yaxt = "n", lwd=2)
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means)), yaxt = "n", lwd=2)
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means)), 1,8, yaxt = "n", lwd=2)
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means)), 1,8, lwd=2)
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means)), 1,8, yaxt = "n", lwd=2)
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means)), 1,8, yaxt = "n", lwd=2)
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means)), 1,8, lwd=2)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
#x <- seq(1, 8, length = 1000)
#y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means))*200, add = TRUE, yaxt = "n", lwd=2)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
#x <- seq(1, 8, length = 1000)
#y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means))*400, add = TRUE, yaxt = "n", lwd=2)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
#x <- seq(1, 8, length = 1000)
#y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means))*450, add = TRUE, yaxt = "n", lwd=2)
#Histgram of sample means
hist(sample.means)
#Normal distribution placed over
set.seed(42)
#x <- seq(1, 8, length = 1000)
#y <- dnorm(x, mean = mean(sample.means), sd = sd(sample.means))
curve(dnorm(x, mean = mean(sample.means), sd = sd(sample.means))*500, add = TRUE, yaxt = "n", lwd=2)
sd(sample.means)
count(sample.means[sample.means < (mean(sample.means)-sd(sample.means)) &
sample.means > (mean(sample.means)+sd(sample.means))])
length(sample.means[sample.means < (mean(sample.means)-sd(sample.means)) &
sample.means > (mean(sample.means)+sd(sample.means))])
sample.means[sample.means < (mean(sample.means)-sd(sample.means)) &
sample.means > (mean(sample.means)+sd(sample.means))]
sample.means[sample.means > (mean(sample.means)-sd(sample.means)) &
sample.means < (mean(sample.means)+sd(sample.means))]
length(sample.means[sample.means > (mean(sample.means)-sd(sample.means)) &
sample.means < (mean(sample.means)+sd(sample.means))])
length(sample.means[sample.means > (mean(sample.means)-sd(sample.means)) &
sample.means < (mean(sample.means)+sd(sample.means))]) /
length(sample.means)
length(sample.means[sample.means > (mean(sample.means) - sd(sample.means)) &
sample.means < (mean(sample.means) + sd(sample.means))]) /
length(sample.means)
length(sample.means[sample.means > (mean(sample.means) - 2 * sd(sample.means)) &
sample.means < (mean(sample.means) + 2 * sd(sample.means))]) /
length(sample.means)
